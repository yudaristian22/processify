{
  "version": 2,
  "builds": [
    {
      "src": "waitress-serve --port $PORT app:app",
      "use": "@vercel/python"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "waitress-serve --port $PORT app:app" }]
}
